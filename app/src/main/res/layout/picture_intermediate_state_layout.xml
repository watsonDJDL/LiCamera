<?xml version="1.0" encoding="utf-8"?>
<!--拍照后，编辑前的照片显示中间态 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/picture_intermediate_fragment_layout">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/picture_top_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <ImageView
            android:id="@+id/character_recognition_btn"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:background="@color/transparent"
            android:src="@mipmap/character_recognition_icon"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toLeftOf="@id/picture_recognition_btn"/>

        <ImageView
            android:id="@+id/picture_recognition_btn"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:background="@color/transparent"
            android:src="@mipmap/picture_recognition_icon"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toRightOf="@id/character_recognition_btn"
            app:layout_constraintRight_toRightOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <ImageView
        android:id="@+id/picture_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>

    <FrameLayout
        android:layout_gravity="bottom"
        android:id="@+id/picture_intermediate_bottom_area"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp">
        <ImageButton
            android:id="@+id/delete_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/round_button"
            android:src="@mipmap/delete_icon"
            android:layout_gravity="start" />
        <ImageButton
            android:id="@+id/select_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/round_button"
            android:src="@mipmap/select_icon"
            android:layout_gravity="end"/>
    </FrameLayout>


</FrameLayout>